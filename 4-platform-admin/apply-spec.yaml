applySpecVersion: 1
spec:
  configSync:
    enabled: true
    sourceFormat: unstructured
    syncRepo: https://github.com/askmeegs/cymbalbank-policy
    syncBranch: main
    secretType: none
    policyDir: /
  policyController:
    enabled: true